import styled from 'styled-components'

export const StyledBackToStartContainer = styled.div`
  width: 95px;
  height: 20px;
  display: flex;
  justify-content: space-between;

  i, h1 {
    color: var(--orange);
  }

  h1 {
    font-size: 12px;
    align-self: center;
  }

  i {
    font-size: 20px;
  }
`
